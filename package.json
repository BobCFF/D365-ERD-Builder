{
  "name": "d365-erd-builder",
  "version": "0.0.0",
  "private": true,
  "description": "In-browser Dynamics 365 / Dataverse customizations.xml ERD builder (single self-contained page).",
  "license": "MIT",
  "scripts": {
    "build": "python3 build-d365.py",
    "serve": "python3 -m http.server 8100",
    "test": "node tests/run.cjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
